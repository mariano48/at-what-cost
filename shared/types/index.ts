// Shared event and DTO types will live here as labs are implemented.
// e.g. Lab 02 will add `payment-completed.event.ts` for the async contract
// between payments-api and its subscribers (orders, notifications, audit).
export {};
